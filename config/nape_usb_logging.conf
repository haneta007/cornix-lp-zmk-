# The USB CDC port is a log console in this temporary diagnostic build.
# ZMK Studio's RPC endpoint uses its own CDC port in the normal builds.
CONFIG_ZMK_STUDIO=n
CONFIG_LOG_BUFFER_SIZE=2048
CONFIG_ZMK_NAPE_BOND_DIAGNOSTICS=y
